import { Verificationstatus } from './verificationstatus';

describe('Verificationstatus', () => {
  it('should create an instance', () => {
    expect(new Verificationstatus()).toBeTruthy();
  });
});
